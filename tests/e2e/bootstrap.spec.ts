import { test } from "@playwright/test";

test("bootstrap e2e scaffold placeholder", async () => {
  test.skip(true, "Replace this placeholder once the web and auth flows are wired for real e2e coverage.");
});
