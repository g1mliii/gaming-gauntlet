{
  "name": "@gaming-gauntlet/edge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cf:types": "wrangler types src/worker-configuration.d.ts --config ../../wrangler.toml",
    "cf:types:check": "wrangler types src/worker-configuration.d.ts --config ../../wrangler.toml --check",
    "dev": "wrangler dev --config ../../wrangler.local.toml --port 8787",
    "build": "wrangler deploy --dry-run --config ../../wrangler.toml",
    "deploy": "wrangler deploy --config ../../wrangler.toml"
  },
  "dependencies": {
    "@gaming-gauntlet/contracts": "0.1.0",
    "jose": "^6.0.8"
  }
}
